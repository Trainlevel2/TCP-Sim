The following need to be checked!

router.h	packet* receivePacket(packet* pptr,link* lptr);
router.cpp	packet* receivePacket(link* lptr){ return lptr->currentPkt; }

flow.h		void receiveAck(packet p);
flow.cpp	void flow::receiveAck(packet* p) 

link.h		link(int maxT, node* src, node* dest);
link.cpp	link::link(int maxT, int buff, node* source, node* destination){

packet.h	packet(int dat, node* src, node* dst);
packet.cpp	packet::packet(int dat, node* source, node* destination) {
		->dat, source, destination versus data, src, dest

4/18/16
Darwin Huang: Output graphs
Sehr Ahmed: Adapt routers match hosts
Eric Mendoza-Conner: Implement TCP [implement congestion avoidance (linear), fast retransmit, change triple ACK to cwnd = 1)
Kirk Cameron: Routing Tables
